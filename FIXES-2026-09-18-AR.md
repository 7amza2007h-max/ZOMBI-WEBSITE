# ZOMBI Dashboard Fixes — 2026-09-18

## إصلاح "البوت غير مضاف"
- تم إلغاء الفحص القديم الذي كان يحوّل أي خطأ من Discord API إلى "البوت غير مضاف".
- Dashboard يفحص قائمة سيرفرات البوت دفعة واحدة بدل طلب منفصل لكل سيرفر.
- تمت إضافة Heartbeat من البوت كـ fallback، ويستخدم `guildIds` للتأكد من السيرفرات الموجودة فيها ZOMBI.
- إذا تعطل BOT_TOKEN أو Discord API لن يظهر السيرفر كأنه غير مضاف بشكل كاذب؛ تظهر حالة "تعذر التحقق" مع سبب الإعداد.
- زر إضافة البوت يدعم `DISCORD_BOT_CLIENT_ID` إذا كان تطبيق OAuth مختلفًا عن تطبيق البوت.

## إعدادات Render المهمة
يجب أن يكون `BOT_TOKEN` هو نفس توكن ZOMBI Bot الموجود على Monkey Network.
إذا كان OAuth والبوت تطبيقين مختلفين، أضف:
`DISCORD_BOT_CLIENT_ID=<Application ID الخاص بالبوت>`
وإلا يمكن تركه فارغًا وسيستخدم `DISCORD_CLIENT_ID`.

إذا كان البوت والموقع يستخدمان نفس `DATABASE_URL`، Heartbeat سيعمل مباشرة من قاعدة البيانات. وإذا لم تكن قاعدة البيانات مشتركة، يوجد endpoint آمن `/api/bot-sync/heartbeat` للمزامنة.

Start command: `node server.js`
