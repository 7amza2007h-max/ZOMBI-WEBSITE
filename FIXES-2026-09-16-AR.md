# ZOMBI — إصلاحات 2026-09-16

تم تنفيذ الإصلاحات بشكل محافظ بدون حذف بيانات السيرفر أو تغيير الأرصدة/العصابات/التذاكر أو ملف `.env` في نسخة البوت الكاملة.

## ما تم إصلاحه

- إصلاح خطأ حفظ الرتبة التلقائية `guild is not defined` في Dashboard.
- التحقق من أن Auto Role المختارة موجودة في السيرفر وليست رتبة managed، بدون إنشاء رتبة جديدة.
- توحيد `planPolicy` بين البوت والموقع حتى لا تختفي إعدادات أو صلاحيات الموسيقى عند الحفظ.
- إضافة إعدادات الموسيقى للـ Dashboard وربط الحفظ بها: تشغيل النظام، السماح للجميع، رتب التحكم، الصوت الافتراضي، وقت الخروج التلقائي، ورسالة التشغيل.
- إصلاح حفظ checkboxes والمحدد المتعدد في قسم الموسيقى حتى يمكن إطفاء الخيارات ومسح رتب التحكم فعليًا.
- منع الخطة المقفلة من الكتابة فوق إعدادات الموسيقى القديمة.
- إضافة/توحيد حدود الموسيقى حسب الخطة (`musicQueueSize`, `musicMaxVolume`, `musicMaxTrackMinutes`).
- توحيد حدود City Director بين البوت والموقع إلى Free=5 / Premium=50 / Premium+=100، مع الحفاظ على الحقول المتقدمة للقوالب.
- إضافة تطبيع آمن لإعدادات `cfg.music` في مخزن إعدادات السيرفر.
- إضافة `.gitignore` للبوت وتوسيع `.gitignore` للموقع لمنع رفع `.env` وملفات dependencies/cache عن طريق الخطأ.

## ما لم يتم تغييره عمدًا

- ملف `.env` داخل **نسخة البوت الكاملة** بقي كما هو حرفيًا.
- مجلد `data/` في البوت بقي مطابقًا للنسخة الأصلية؛ لم يتم تغيير أرصدة أو إعدادات أو معلومات أعضاء.
- محرك تشغيل الصوت الحالي لم يتم استبداله بمكتبة YouTube جديدة؛ تغيير طبقة البث الآن قد يسبب كسر النظام. تم إصلاح ربط Dashboard والخطط بدون تغيير مسار الصوت الموجود.
- ملفات النسخ الاحتياطية القديمة ذات الأسماء مثل `(1)` و`(2)` بقيت في النسخة الكاملة حتى لا يتم حذف أي شيء قد تحتاجه لاحقًا؛ التشغيل يعتمد على الملفات الأساسية غير المرقمة.

## الاختبارات

- `npm run check` نجح للموقع والبوت.
- فحص Syntax لكل ملفات JavaScript الفعلية نجح (مع استثناء `cloudflare-worker.js` لأنه Cloudflare Worker وليس Node CommonJS).
- Website smoke test نجح وفتح الصفحة الرئيسية على سيرفر محلي تجريبي.
- اختبارات تطبيع Music / Plan Policy / City Director / Access Policy نجحت.
- تم التحقق أن `.env` في البوت لم يتغير، وأن جميع ملفات `data/` مطابقة للنسخة الأصلية.
