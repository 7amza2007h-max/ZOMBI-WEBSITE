{
  "name": "zombi-dashboard-website",
  "version": "9.14.0",
  "description": "ZOMBI V9.14 — City Director, interactive Server Guide, events, plans and dashboard controls",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check paymentStore.js && node --check sharedStore.js && node --check guildStore.js && node --check planPolicy.js && node --check gameDefaults.js && node --check discordCommands.js && node --check pricingView.js && node --check site/dashboard.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "pg": "^8.13.1"
  }
}
